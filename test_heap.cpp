#include "6heap.h"
/*
int main() {
    BinaryHeap<int> heap;
	heap.Insert(22);
	heap.Insert(12);
	heap.Insert(7);
	heap.Insert(1);
	heap.PrintItems();

	vector<double> dvec;
	int i;
	for (i = 10; i > 0; --i)
		dvec.push_back(i);
	BinaryHeap<double> dheap(dvec);
	dheap.PrintItems();

	heap.DeleteMin();
	heap.PrintItems();

	dheap.DeleteMin();
	dheap.DeleteMin();
	dheap.PrintItems();
	return 0;
}
*/